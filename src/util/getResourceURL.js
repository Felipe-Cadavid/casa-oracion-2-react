import Database from "./Database";

function getResourceURL(){
    const storageRef = Databse.ref(`institute-resources/${name}`)
    return new Promise((resolve, reject) => {
        resolve('xdxdxdxd')
    });
}

export default getResourceURL;